# Screenshot placeholders

Add real dashboard captures here (e.g., `overview.png`, `detail.png`) and update paths in the root README as needed.

Binary assets are intentionally omitted from version control in this environment.
